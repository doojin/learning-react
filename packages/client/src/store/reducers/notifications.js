const initialState = {
  loading: {
    message: ''
  },
  error: {
    message: ''
  }
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
